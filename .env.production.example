# Host SSH connection
DEPLOY_HOST=achildrenmile@host-node-01

# Remote directory on the host
REMOTE_DIR=/home/achildrenmile/apps/bosarsalog

# Docker container settings
CONTAINER_NAME=bosarsalog
IMAGE_NAME=bosarsalog:latest
CONTAINER_PORT=3416:3000

# Public URL for health check
SITE_URL=https://bosarsalog.oeradio.at/

# JWT secret for production (change this!)
JWT_SECRET=change-me-to-a-random-string
