Implement the following plan:

# Issue #8: Download Auswertung — Server-seitiges PDF

## Context
"Download Auswertung komplett" nutzt `html-to-image` (`toPng()`), das den gesamten DOM klont — inkl. SVG-Karte, Chart.js-Diagramme, große Tabellen. Bei umfangreichen Auswertungen crasht der Browser (500MB+ RAM, Main Thread blockiert). Lösung: PDF-Generierung auf dem Server mit PDFKit (pure JS, kein Chromium nötig, läuft auf Alpine).

**Scope:** Nur die "Download Auswertung komplett"-Buttons auf beiden Seiten werden auf PDF umgestellt. Einzelne Chart-Downloads (Karte, Balken, Torte) bleiben als PNG via html-to-image.

## Ansatz: PDFKit (pure JavaScript)

- Kein Chromium/Puppeteer nötig → kein Dockerfile-Umbau
- SVG-Pfade der Austria-Karte können direkt mit PDFKit gezeichnet werden
- Bar-/Pie-Charts werden mit PDFKit-Zeichenprimitiven (Rechtecke/Kreissegmente) nachgebaut
- Tabellen als strukturierter Text

## Änderungen

### 1. Dependency hinzufügen
```
cd server && npm install pdfkit @types/pdfkit
```

### 2. Neue Datei: `server/src/services/pdf.ts`

Shared PDF-Generator mit Helper-Funktionen:

- `renderHeader(doc, title, subtitle)` — BOS-ARSA Branding (dunkelblaue Leiste, Titel, Slogan)
- `renderSummaryCards(doc, stats, exerciseCount?)` — Teilnehmer/Rapporte/Übungen Zahlen
- `renderPerRepeater(doc, perRepeater)` — Repeater-Übersicht Grid
- `renderExerciseTable(doc, exercises)` — Übungsliste (nur aggregiert)
- `renderAustriaMap(doc, blStats)` — SVG-Pfade aus AustriaMap.tsx, Farben nach Teilnehmerzahl
- `renderBarChart(doc, chartData)` — Balkendiagramm mit Rechtecken
- `renderPieChart(doc, chartData)` — Tortendiagramm mit Kreissegmenten
- `renderBezirkTable(doc, bezirkStats)` — Bezirk/BL Tabelle mit Hierarchie
- `renderBlTable(doc, blStats)` — Bundesland-Tabelle (Fallback ohne Bezirke)
- `renderParticipants(doc, participants)` — Teilnehmer-Liste
- `renderFooter(doc)` — "oeradio.at · bosarsa.oeradio.at"

Austria-Karte: SVG-Pfaddaten aus `AustriaMap.tsx` extrahieren und als Konstante in pdf.ts ablegen. PDFKit zeichnet die Pfade mit `doc.path(d).fill(color)`. Farblogik (Heatmap nach Teilnehmern) wird 1:1 übernommen.

### 3. Neue Datei: `server/src/routes/pdf.ts`

Zwei Endpunkte:

**GET `/api/v1/pdf/reports?from=...&to=...&types=...`** — Aggregierte Auswertung
- Nutzt gleiche DB-Queries wie `reports.ts:/stats`
- Ruft PDF-Generator auf mit exercises + stats
- Response: `application/pdf` mit Content-Disposition

**GET `/api/v1/pdf/exercises/:id`** — Einzel-Übung Auswertung
- Nutzt gleiche DB-Queries wie `exercises.ts:/:id/stats`
- Ruft PDF-Generator auf mit exercise + stats
- Response: `application/pdf` mit Content-Disposition

### 4. Route registrieren: `server/src/index.ts`

```ts
import { pdfRouter } from './routes/pdf.js';
app.use('/api/v1/pdf', authMiddleware, pdfRouter);
```

### 5. Client: `AggregatedReportsPage.tsx`

- `downloadFullPage()` ersetzen: statt `toPng()` → `fetch('/api/v1/pdf/reports?from=...&to=...&types=...')` + Blob-Download
- Dateiname: `BOS-ARSA_Auswertung_{from}_{to}.pdf`
- `toPng`-Import kann bleiben (wird für einzelne Chart-Downloads weiter genutzt)

### 6. Client: `ReportsPage.tsx`

- `downloadFullPage()` ersetzen: statt `toPng()` → `fetch('/api/v1/pdf/exercises/${id}')` + Blob-Download
- Dateiname: `BOS-ARSA_Auswertung_{date}.pdf`

## PDF-Layout (A4 Querformat)

```
┌─────────────────────────────────────────────────┐
│ ██ BOS-ARSA Log  ·  Im Sinne der Sicherheit    │  Header
│    Auswertung 01.01.2026 – 31.03.2026          │
├─────────────────────────────────────────────────┤
│  [42 Teiln.]  [128 Rapp.]  [12 Übungen]  [Ø 3] │  Summary
├─────────────────────────────────────────────────┤
│  Schöckl 2m: 35  |  Lachtal: 28  |  OE-Link: 65│  Per-Repeater
├─────────────────────────────────────────────────┤
│  Übungsliste (Datum | Typ | Name | Teiln | Rap) │  Exercises
├──────────────────────┬──────────────────────────┤
│  [Austria Map SVG]   │  [Bar Chart]             │  Visuals
│                      │  [Pie Chart]             │
├──────────────────────┴──────────────────────────┤
│  Bundesland/Bezirk | Teilnehmer | Rapporte      │  Table
├─────────────────────────────────────────────────┤
│  # | Rufzeichen | Name | Bezirk | BL | Rapp.   │  Participants
├─────────────────────────────────────────────────┤
│  oeradio.at · bosarsa.oeradio.at                │  Footer
└─────────────────────────────────────────────────┘
```

## Dateien

| Datei | Änderung |
|-------|----------|
| `server/package.json` | `pdfkit` + `@types/pdfkit` hinzufügen |
| `server/src/services/pdf.ts` | **NEU** — PDF-Generator mit allen Render-Funktionen |
| `server/src/routes/pdf.ts` | **NEU** — API-Endpunkte für PDF-Download |
| `server/src/index.ts` | PDF-Route registrieren |
| `client/src/pages/AggregatedReportsPage.tsx` | `downloadFullPage` → Server-PDF-Fetch |
| `client/src/pages/ReportsPage.tsx` | `downloadFullPage` → Server-PDF-Fetch |

## Verifikation
1. `nix-shell -p nodejs --run "npm run build"` kompiliert sauber
2. Aggregierte Auswertung: "Download Auswertung" → PDF wird sofort heruntergeladen, kein Browser-Freeze
3. Einzel-Übung: "Download Auswertung" → PDF mit Übungsdaten
4. Einzelne Chart-Downloads (Karte, Balken, Torte PNG) funktionieren weiterhin
5. PDF enthält: Header, Summary, Karte, Charts, Tabellen, Teilnehmer, Footer
6. Deploy mit `./deploy-production.sh`


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/achildrenmile/.REDACTED.jsonl

---

deploy mit ./deploy-production.sh

---

ja, commit, push und neu deployen

---

österreich karte ist schwarz.

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. The user provided a detailed plan for Issue #8: Server-side PDF generation to replace client-side html-to-image for "Download Auswertung komplett" buttons.

2. I read multiple files to understand the codebase:
   - server/src/index.ts - route registration
   - server/src/routes/reports.ts - aggregated stats queries
   - server/src/routes/exercises.ts - exercise stats queries
   - client/src/pages/AggregatedReportsPage.tsx - downloadFullPage function using toPng
   - client/src/pages/ReportsPage.tsx - downloadFullPage function using toPng
   - client/src/components/AustriaMap.tsx - SVG path data, color logic, BL_PATHS, NEIGHBOR_PATHS
   - client/src/services/api.ts - apiFetch helper
   - client/src/types/stats.ts - TypeScript interfaces
   - server/package.json - dependencies

3. I created tasks and implemented:
   - Task 1: Install pdfkit dependency
   - Task 2: Create server/src/services/pdf.ts
   - Task 3: Create server/src/routes/pdf.ts
   - Task 4: Register PDF route in server/src/index.ts
   - Task 5: Update client download functions
   - Task 6: Build and verify

4. First deployment failed silently because code wasn't committed/pushed - Docker used cached layers.

5. User said "then update markdown" - I updated CHANGELOG-2026-02-24.md with Issue #8 details.

6. User said "ja, commit, push und neu deployen" - I committed, pushed, and redeployed successfully.

7. User reported "österreich karte ist schwarz" - Two bugs found:
   - getHeatColor returned `rgb(r,g,b)` CSS format which PDFKit doesn't understand → fixed to hex
   - `.fill(fill).stroke(WHITE)` doesn't work in PDFKit → changed to `.fillAndStroke(fillColor, WHITE)`

8. User reported "teilnehmer rapporte liste ist auch grauenhaft" - The table layouts were broken because PDFKit's `doc.text()` moves `doc.y` after each call, so multiple columns per row caused Y position drift.

9. I rewrote the entire pdf.ts with a `textAt()` helper that always uses `lineBreak: false` to prevent Y drift. All table functions were rewritten to:
   - Save rowY before each row
   - Use textAt() for all text positioning
   - Manually set doc.y after each row

10. The file was written but NOT yet built, committed, pushed, or deployed. That's where we left off.

Key errors:
- rgb() color format not supported by PDFKit → hex format
- fill().stroke() chaining doesn't work → fillAndStroke()
- doc.text() without lineBreak:false moves cursor → textAt() helper

The most recent state: pdf.ts was rewritten and saved, but needs build + commit + push + deploy.

Summary:
1. Primary Request and Intent:
   The user requested implementation of Issue #8: Replace client-side `html-to-image` (`toPng()`) based "Download Auswertung komplett" with server-side PDF generation using PDFKit. Only the full-page download buttons should change; individual chart PNG downloads (Karte, Balken, Torte) remain unchanged. The user provided a detailed implementation plan covering new files, modified files, PDF layout, and verification steps. After implementation, the user requested: CHANGELOG update, git commit+push, and deployment via `./deploy-production.sh`. After deployment, the user reported two visual bugs in the generated PDF that needed fixing.

2. Key Technical Concepts:
   - PDFKit (pure JS PDF generation, no Chromium needed)
   - SVG path rendering in PDFKit via `doc.path(d).fillAndStroke()`
   - PDFKit text positioning: `lineBreak: false` is critical for tabular layouts
   - PDFKit color format: only accepts hex colors, NOT CSS `rgb()` notation
   - PDFKit `fillAndStroke(fillColor, strokeColor)` vs broken `fill().stroke()` chaining
   - NixOS environment: `nix-shell -p nodejs --run "..."` for npm/node commands
   - Deployment: `./deploy-production.sh` deploys via SSH to remote, builds Docker there
   - Must commit+push before deploy (Docker pulls from git on remote)
   - Monorepo: `/client` (React/Vite) + `/server` (Express/TypeScript/SQLite)
   - Austrian Bundesland SVG paths from AustriaMap.tsx component

3. Files and Code Sections:

   - **server/package.json** — Added `pdfkit` and `@types/pdfkit` dependencies
     ```json
     "pdfkit": "^0.x.x",
     "@types/pdfkit": "^0.x.x"
     ```

   - **server/src/services/pdf.ts** — NEW file, the core PDF generator. Was rewritten twice due to bugs. Final version uses `textAt()` helper for all text positioning:
     ```typescript
     function textAt(doc: PDFKit.PDFDocument, str: string, x: number, y: number, opts?: PDFKit.Mixins.TextOptions) {
       doc.text(str, x, y, { lineBreak: false, ...opts });
     }
     ```
     Key functions: `renderHeader`, `renderSummaryCards`, `renderPerRepeater`, `renderExerciseTable`, `renderAustriaMap`, `renderBarChart`, `renderPieChart`, `renderBezirkTable`, `renderBlTable`, `renderParticipants`, `renderFooter`.
     Public API: `generateAggregatedPdf()` and `generateExercisePdf()`.
     Contains full Austrian Bundesland SVG path data in `BL_PATHS` constant (codes '01'-'09').
     Heat color function returns hex:
     ```typescript
     function getHeatColor(value: number, max: number): string {
       if (max === 0 || value === 0) return '#e5e7eb';
       const t = value / max;
       const r = Math.round(194 + (30 - 194) * t);
       const g = Math.round(213 + (58 - 213) * t);
       const b = Math.round(236 + (95 - 236) * t);
       return '#' + [r, g, b].map(c => c.toString(16).padStart(2, '0')).join('');
     }
     ```
     Map rendering uses `fillAndStroke`:
     ```typescript
     doc.path(pathData.d).lineWidth(2 / scale).fillAndStroke(fillColor, WHITE);
     ```
     All table rows save Y position and use textAt:
     ```typescript
     const rowY = doc.y;
     // ... render columns with textAt at rowY ...
     doc.y = rowY + 11;  // advance manually
     ```

   - **server/src/routes/pdf.ts** — NEW file with two endpoints:
     - `GET /api/v1/pdf/reports?from=...&to=...&types=...` — aggregated PDF
     - `GET /api/v1/pdf/exercises/:id` — single exercise PDF
     Both use same DB queries as reports.ts and exercises.ts, pipe PDFDocument to response with `application/pdf` content type.

   - **server/src/index.ts** — Added import and route registration:
     ```typescript
     import { pdfRouter } from './routes/pdf.js';
     app.use('/api/v1/pdf', authMiddleware, pdfRouter);
     ```

   - **client/src/pages/AggregatedReportsPage.tsx** — Replaced `downloadFullPage` from toPng to server PDF fetch:
     ```typescript
     const downloadFullPage = async () => {
       setExporting(true);
       try {
         const token = localStorage.getItem('token');
         const typesParam = selectedTypes.size > 0 ? `&types=${Array.from(selectedTypes).map(encodeURIComponent).join(',')}` : '';
         const res = await fetch(`/api/v1/pdf/reports?from=${from}&to=${to}${typesParam}`, {
           headers: token ? { Authorization: `Bearer ${token}` } : {},
         });
         if (!res.ok) throw new Error();
         const blob = await res.blob();
         const url = URL.createObjectURL(blob);
         const link = document.createElement('a');
         link.href = url;
         link.download = `BOS-ARSA_Auswertung_${from}_${to}.pdf`;
         link.click();
         URL.revokeObjectURL(url);
       } catch {} finally {
         setExporting(false);
       }
     };
     ```

   - **client/src/pages/ReportsPage.tsx** — Same downloadFullPage replacement pattern, fetching `/api/v1/pdf/exercises/${id}`. Button label changed from "PNG — Download Auswertung" to "PDF — Download Auswertung".

   - **CHANGELOG-2026-02-24.md** — Added Issue #8 section documenting the server-side PDF implementation.

   - **client/src/components/AustriaMap.tsx** — Read only (not modified). Source for SVG path data (`BL_PATHS`, `NEIGHBOR_PATHS`), color logic (`getColor`), and display constants (`BL_SHORT`, `BL_NAMES`).

   - **client/src/types/stats.ts** — Read only. Defines `BezirkStat`, `Participant`, `RepeaterStat`, `BlStat`, `Stats` interfaces.

   - **client/src/services/api.ts** — Read only. `apiFetch()` helper with token auth and 401 redirect.

4. Errors and fixes:
   - **Austria map rendered black in PDF:**
     - Root cause 1: `getHeatColor()` returned CSS `rgb(r,g,b)` format which PDFKit doesn't understand → Fixed to return hex format `#rrggbb`
     - Root cause 2: `doc.path(d).fill(fill).stroke(WHITE)` — after `fill()`, the path is consumed so `stroke()` has nothing to stroke → Fixed to `doc.path(d).fillAndStroke(fillColor, WHITE)`
     - User feedback: "österreich karte ist schwarz"

   - **Participant/report tables had broken layout:**
     - Root cause: PDFKit's `doc.text()` automatically advances `doc.y` after each call. When rendering multiple columns per row, each `text()` call shifted Y down, causing cascading misalignment
     - Fix: Created `textAt()` helper that always passes `lineBreak: false`, and manually tracked/set `doc.y` per row using saved `rowY` variable
     - User feedback: "teilnehmer rapporte liste ist auch grauenhaft"

   - **First deployment had no effect (Docker cached):**
     - Root cause: Code wasn't committed/pushed before running `./deploy-production.sh`. The script does `git pull` on remote, so without push, Docker saw no changes
     - Fix: Committed, pushed, then redeployed — second deploy correctly rebuilt with new files

5. Problem Solving:
   - Successfully implemented full server-side PDF generation replacing browser-heavy html-to-image approach
   - Identified and fixed PDFKit-specific issues (color format, path chaining, text positioning)
   - Learned deployment requires commit+push first since remote builds from git
   - Full rewrite of pdf.ts was needed to fix systematic text positioning issues across all table render functions

6. All user messages:
   - "Implement the following plan: [detailed Issue #8 plan for server-side PDF]"
   - "deploy mit ./deploy-production.sh"
   - "then update markdown"
   - "ja, commit, push und neu deployen"
   - "österreich karte ist schwarz."
   - "teilnehmer rapporte liste ist auch grauenhaft"

7. Pending Tasks:
   - Build the rewritten pdf.ts (`nix-shell -p nodejs --run "npm run build"`)
   - Commit the fixes
   - Push to remote
   - Redeploy with `./deploy-production.sh`
   - Verify the PDF output looks correct (map colors, table layouts)

8. Current Work:
   I had just finished rewriting the entire `server/src/services/pdf.ts` file to fix two reported bugs:
   1. Black Austria map (hex colors + fillAndStroke)
   2. Broken table layouts (textAt helper with lineBreak:false)
   
   The file was saved via Write tool but has NOT yet been built, committed, pushed, or deployed. The user's last explicit requests were reporting the two visual bugs. The fix has been written but not yet verified or deployed.

9. Optional Next Step:
   Build the project to verify the rewritten pdf.ts compiles cleanly (`nix-shell -p nodejs --run "npm run build"`), then commit, push, and redeploy with `./deploy-production.sh` — following the same workflow the user previously requested: "ja, commit, push und neu deployen".

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/achildrenmile/.REDACTED.jsonl
Please continue the conversation from where we left it off without asking the user any further questions. Continue with the last task that you were asked to work on.